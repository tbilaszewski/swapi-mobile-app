import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 20,
    margin: 10,
    flexGrow: 0,
  },
  winnerCard: {
    borderColor: "green",
    borderWidth: 5,
  },
});
